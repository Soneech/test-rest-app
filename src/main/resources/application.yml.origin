spring:
  datasource:
    driver-class-name:
    url:
    username:
    password:
  jpa:
    properties:
      hibernate:
        dialect:
        show_sql: true
  mvc:
    hiddenmethod:
      filter:
        enabled: true